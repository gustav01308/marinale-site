'use client';

// Animações de Intersection Observer removidas para evitar elementos invisíveis ao carregar.
// Mantido como stub para não quebrar o import em layout.tsx.
export default function AnimationProvider() {
  return null;
}
